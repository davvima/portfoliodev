---
import HeroButtons from "./HeroButtons"
---

<section class="h-screen max-h-[1000px] flex items-center justify-center relative overflow-hidden">
  <div class="text-center z-10 px-4">
    <div class="transition-transform duration-1000 ease-out translate-y-0">
      <h2 class="text-5xl md:text-7xl font-light mb-6 tracking-tight">
        <span class="block">Hello, I'm</span>
        <span
          class="block font-medium bg-gradient-to-r from-slate-400 to-slate-900
           dark:from-white dark:to-slate-400 bg-clip-text text-transparent"
        >
          David Freites
        </span>
      </h2>
    </div>

    <div class="transition-all duration-1000 ease-out">
      <p class="text-xl md:text-2xl mb-8 font-light max-w-2xl mx-auto leading-relaxed">
        Creative developer crafting digital experiences with attention to detail
      </p>
    </div>

    <HeroButtons client:load />
  </div>

  {/* Scroll indicator */}
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2">
    <div
      class="w-6 h-10 border border-gray-500 dark:border-gray-300 rounded-full flex justify-center"
    >
      <div class="w-1 h-3 bg-gray-600 dark:bg-gray-400 rounded-full mt-2 animate-bounce"></div>
    </div>
  </div>
</section>
